

decoding.sh  - skript
input.base64 - Ein Payload in base64 vom Sensor zum decodieren / ein zusammengefügtes Telegramm mit allen Werten

hex - Zwischenspeicher in HEX, Zeilenweise Ausgabe
hex2 - Zwischenspeicher in HEX mit space separiert
